import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const TabBarButton = ({item}:any) => {
  return (
    <View>
      <Text></Text>
    </View>
  )
}

export default TabBarButton

const styles = StyleSheet.create({})